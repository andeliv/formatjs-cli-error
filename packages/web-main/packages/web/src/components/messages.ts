import { defineMessages } from 'react-intl';

export const trns = defineMessages({
    messageId: {
        id: 'comp.id',
        defaultMessage: 'Message text 1',
        description: '#6388'
    }
});
